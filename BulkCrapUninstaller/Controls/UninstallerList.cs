using System;
using System.Collections.Generic;
using System.ComponentModel;
using System.Drawing;
using System.Data;
using System.Linq;
using System.Text;
using System.Windows.Forms;

namespace BulkCrapUninstaller.Controls
{
    public partial class UninstallerList : UserControl
    {
        public UninstallerList()
        {
            InitializeComponent();
            /*
            uninstallerObjectListView.DataBindings.Add(nameof(uninstallerObjectListView.SelectedItems), treeListView1,
                nameof(uninstallerObjectListView.SelectedItems));
            uninstallerObjectListView.DataBindings.Add(nameof(uninstallerObjectListView.), treeListView1,
                nameof(uninstallerObjectListView.SelectedItems));*/
        }
    }
}
